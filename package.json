{
  "name": "sih26043",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "api": "python -m uvicorn api:app --app-dir ai --host 127.0.0.1 --port 8000",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend run start",
    "lint": "npm --prefix frontend run lint"
  }
}